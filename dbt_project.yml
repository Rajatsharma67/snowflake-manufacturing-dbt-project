name: snowflake-manufacturing-dbt-project
version: 1.0
config-version: 2

profile: snowflake-manufacturing-dbt-project

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"

models:
  snowflake-manufacturing-dbt-project:
    staging:
      materialized: view
    intermediate:
      materialized: ephemeral
    marts:
      materialized: table